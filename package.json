{
  "name": "densifyserver",
  "version": "0.0.0",
  "description": "server for the densify app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@nish$ingh",
  "license": "ISC",
  "dependencies": {
    "ws": "^3.2.0"
  }
}
